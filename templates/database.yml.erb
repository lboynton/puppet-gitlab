#
# PRODUCTION
#
production:
  adapter: <% if db_type == 'mysql' %>mysql2<% else %>postgresql<% end %>
  encoding: utf8
  reconnect: false
  database: <%= db_name %>
  pool: 5
  username: "<%= db_username %>"
  password: "<%= db_password %>"
  host: <%= db_server %>
